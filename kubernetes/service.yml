apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: microservices
    name: frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: LoadBalancer
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: ml-service
# spec:
#   selector:
#     app: microservices
#     name: ml
#   ports:
#   - name: http
#     port: 8080
#     targetPort: 8080
#   type: ClusterIP
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: safepoint_tracker-service
# spec:
#   selector:
#     app: microservices
#     name: safepoint_tracker
#   ports:
#   - name: http
#     port: 8080
#     targetPort: 8080
#   type: ClusterIP
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: auth-service
# spec:
#   selector:
#     app: microservices
#     name: auth
#   ports:
#   - name: http
#     port: 8080
#     targetPort: 8080
#   type: ClusterIP